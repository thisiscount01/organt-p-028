# KAMIS 농산물 가격 AI 예측 서비스 — 의존성
# 설치: pip install -r requirements.txt

# ── 웹 서버 (server.py — FastAPI 기반) ──────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# ── AI 엔지니어 참고용 (predictor.py Flask serving — 선택) ──────────────────
# flask>=3.0
# gunicorn>=21.0

# ── 데이터 처리 ───────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24

# ── AI / ML ───────────────────────────────────────────────────────────────────
prophet>=1.1
lightgbm>=4.0
scikit-learn>=1.3

# ── 데이터 수집 (scripts/ 전용) ───────────────────────────────────────────────
playwright>=1.60
